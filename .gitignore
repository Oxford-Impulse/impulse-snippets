# Built plugin zips (rebuilt from source; attach to GitHub Releases instead of committing)
impulse-snippets.zip
impulse-snippets-*.zip

# Extracted release copies used for inspection/staging
impulse-snippets-*/

# Old pre-rewrite prototype, kept locally for reference only
master-code-injector.php
master-code-injector.zip

# Composer dev tooling (reinstall with: php composer.phar install)
vendor/

# OS cruft
Thumbs.db
.DS_Store
desktop.ini
